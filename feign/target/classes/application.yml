server:
  port: 8060
spring:
  application:
    name: feign
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/
  instance:
    prefer-ip-address: false
feign:
    hystrix:
        # 开启熔断器
       enabled: true

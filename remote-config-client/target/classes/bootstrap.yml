spring:
    cloud:
        config:
            name: remote-config-client
            label: master
            discovery:
                enabled: true
                serviceId: remote-config-server
eureka:
    client:
        service-url:
            defaultZone: http://localhost/8761/eureka/
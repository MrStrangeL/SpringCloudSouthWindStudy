spring:
    cloud:
        config:
            name: remote-config-client
            label: master
            discovery:
                enabled: true
                serviceId: remote-config-server
            profile: dev